{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug ffi-bot",
      "type": "node",
      "request": "launch",
      "program": "${workspaceFolder}/src/server.js",
      "cwd": "${workspaceFolder}",
      "envFile": [
        "${workspaceFolder}/.env",
        "${workspaceFolder}/.env.example"
      ],
      "console": "integratedTerminal",
      "restart": true,
      "skipFiles": [
        "<node_internals>/**"
      ]
    }
  ]
}
